var a = 1;

fun fib(n) {
  if (n <= 1) return n;
  return fib(n - 2) + fib(n - 1);
}


fun adder() {
    var i = 0;
    return fun (x) {
        i = i + x;
        print i;
    };
}

var pos = adder();
var neg = adder();

for (var i = 0; i < 5; i = i + 1) {
    pos(1);
    neg(-1);
}

print "fib 13: " + str(fib(13));
